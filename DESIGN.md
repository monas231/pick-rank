# pick-rank 프로젝트 설계 문서

작성: 2026-06-15 / 업데이트: 2026-07-29 (핫딜 기능 추가 + 이미지 처리 방침 v7)

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|------|------|
| 이름 | pick-rank |
| GitHub | github.com/monas231/pick-rank |
| 설명 | 김치(및 향후 식품/생활용품) 평가 및 랭킹 플랫폼 |
| 플랫폼 | Flutter (웹 / Android / iOS) |
| 백엔드 | Supabase |
| 사용자 구조 | 멀티유저 (여러 명이 각자 평가하는 플랫폼) |

**확장 계획:** 현재 김치 전용 → 반응 좋으면 다른 식품, 생활용품으로 확장

---

## 2. 주요 기능

### 랭킹
- 전체 랭킹 (첫 화면 기본) = **종합 만족도 70% + 가격 30%**
- 맛 랭킹 (종합 만족도 평균 높은 순)
- 가격 랭킹 (100g당 가격 오름차순)
- 가성비 랭킹 (맛 점수 / 100g당 가격, 정규화)
- 품평회 랭킹 (aT 대한민국 김치품평회 수상작 기준)

### 핫딜 (딜/할인 정보 — 별도 탭)
- 홈은 **[랭킹] 탭 / [핫딜] 탭** 분리 (성격이 다른 두 콘텐츠)
- 인기 김치의 최저가·이벤트 할인 정보글
- 뽐뿌식 **인기글(핫딜) 노출** — 추천 + 시간 감쇠 랭킹
- 상세는 10장 참고

### 김치 상세 페이지
- 삼각형 레이더 차트 (3개 맛 항목)
- 가격 변동 그래프 (시계열)
- 현재 최저가 정보 (판매처, 링크, 구매방법) — 진행 중 딜 기준
- **관련 핫딜 3~5개** 노출 → 탭하면 핫딜 상세/딜 탭으로 이동 (랭킹 → 딜 연결)
- 하자율 배지 (하자 신고 건수·비율·유형 분포 — 표시 전용)
- 사용자 리뷰 목록

### 사용자 기능
- 리뷰 작성 — **맛평가 / 제품평가 토글 선택** (맛평가 기본 ON, 둘 다 또는 하나만, 최소 하나 필수, 1인 1리뷰 + 수정 가능)
  - **맛평가**: 종합 만족도 별점(**필수**) + 맛 프로필 3항목(**선택** — 세세히 매길 사람은 적으므로 optional)
  - **제품평가(하자 신고)**: 하자 유형(다중선택) + 설명 — 맛 점수에 영향 없음
- 딜/가격 정보 등록 (제목·할인정보·가격·용량·판매처·링크·마감일)
- 딜 추천(upvote) · 딜 댓글
- 리뷰 공감(좋아요)

### 관리자 기능
- 김치 상품 등록/관리
- 품평회 수상 결과 입력 (aT 공식 PDF 참고하여 수동 입력)
- **딜 큐레이션** — 초기 핫딜 정보를 직접 등록 (`is_official`)

---

## 3. 평가 항목 — 맛평가와 제품평가 분리

리뷰 작성 시 사용자는 **맛평가 / 제품평가** 두 종류를 토글로 선택한다. **맛평가가 기본 ON**, 둘 다 켜면 한 페이지에서 함께 작성, 하나만 켜도 되며 **최소 하나는 필수**다.

- **맛평가** = 종합 만족도 별점(**필수**) + 맛 프로필 3축(**선택**) → 3.1, 3.2
- **제품평가** = 제조·배송·포장 등으로 인한 하자 신고 → 3.3

> 맛평가를 켰다 = 별점(종합 만족도)은 반드시 입력. 맛 프로필 3축은 **켜도 안 채워도 됨** — "5개 평가하며 프로필까지 세세히 매길 사람은 많지 않다"는 판단. 채운 사람 것만 모아 레이더 차트에 반영한다.

> 이 분리의 목적: 배송 중 국물이 새거나 상해서 온 사고가 **맛 점수를 오염시키지 않도록** 한다. 하자로 맛을 제대로 못 본 경우 제품평가만 켜서 신고하면 된다(맛 점수는 남기지 않음).

**랭킹에 쓰이는 것은 오직 종합 만족도**이며, 프로필 3축은 "어떤 맛인지" 설명용(레이더 차트·취향 매칭), 하자율은 **표시 전용(랭킹 미반영)** 이다.

### 3.1 종합 만족도 (품질 — 랭킹의 핵심)

| 항목 | 설명 | 척도 |
|------|------|------|
| 종합 만족도 | "전반적으로 얼마나 맛있나" | ⭐ 별점 1.0 ~ 5.0 (0.5 단위, 반점 허용) |

> 여러 사용자의 만족도 평균 = "보편적으로 맛있다"의 지표. 첫 화면 랭킹의 70%를 차지한다.

### 3.2 맛 프로필 (설명 — 삼각형 레이더 차트, 입력은 선택)

**입력 선택사항.** 별점만 남기고 건너뛸 수 있다. 3축은 셋 다 채우거나 아예 비우는 것을 기본으로 한다(부분 입력 시 빈 축은 차트에서 제외). 특정 김치의 프로필 데이터가 부족하면 레이더 차트 대신 "프로필 정보 부족" 안내를 노출한다.

| # | 항목 | 설명 | 범위 |
|---|------|------|------|
| 1 | 매운맛 | 고춧가루 화끈한 정도 | 1~10 |
| 2 | 단맛 | 달달한 정도 | 1~10 |
| 3 | 젓갈맛 | 젓갈 풍미 강도 | 1~10 |

> ⚠️ **프로필 3축은 "좋고 나쁨"이 아니라 "어떤 맛인지"를 나타낸다.** 매운맛 10점은 "좋다"가 아니라 "맵다"는 뜻이므로, 이 값들을 평균 내어 품질 점수로 쓰지 않는다. (안 매운 김치 선호자에겐 낮은 매운맛이 더 좋을 수 있음.) 랭킹은 3.1 종합 만족도로만 계산한다.

> **초기 단순화 방침 (v3):** 진입 장벽을 낮추기 위해 프로필을 3개 항목으로 시작한다.  
> 세 항목(매운맛·단맛·젓갈맛)은 레시피/브랜드 고유의 특성으로, 숙성이 진행돼도 크게 변하지 않아 **제품 속성으로 안정적**이다.  
> 반면 **신맛·아삭함**은 숙성 단계에 따라 계속 변하는(숙성될수록 시어지고 물러짐) 변수라 평가 축에서 제외했다. 반응이 좋으면 향후 별도 메타 정보(예: 리뷰 시점의 숙성도 태그) 형태로 다시 도입 검토.

> 세계김치연구소 관능검사 기준 및 학술 자료 기반으로 선정  
> 감칠맛은 젓갈맛과 중복 가능성이 높아 제외

### 3.3 제품평가 — 하자 신고 (표시 전용, 랭킹 미반영)

"하자율" 개념. 별점이 아니라 **하자 유형을 고르는 신고서** 형태다. 제품평가를 켰다 = "받은 제품에 문제가 있었다"는 뜻.

**입력 항목**

| 항목 | 형식 | 비고 |
|------|------|------|
| 하자 유형 | 다중선택 (1개 이상 필수) | 아래 유형 표 |
| 설명 | text (선택) | |
| 사진 | 이미지 (선택) | **향후** — 초기엔 생략 |

**하자 유형 (책임 주체 태그 병기 → 추후 필터용)**

| 유형 | 주 책임 |
|------|---------|
| 포장 파손 / 국물 누수 | 배송/포장 |
| 변질·상함 (곰팡이, 과발효, 이취) | 제조/유통 |
| 이물질 혼입 | 제조 |
| 제조 불량 (양 부족, 상태 불량) | 제조 |
| 배송 문제 (지연, 미온/해동 등) | 배송 |
| 기타 | - |

**하자율 집계 (표시 전용)**

```
하자율 = 하자 신고된 리뷰 수 / 전체 리뷰 수
```

> **분모는 "전체 리뷰 수"** 다. 문제없던 사람은 맛평가만 하고 지나가도 그 리뷰가 분모에 포함되므로, "제품평가 켠 사람 중에서만" 집계할 때 생기는 **과대평가(불만 있는 사람만 신고하는 편향)를 피한다.** 신고 없음 = 이상 없음으로 간주.

> 상세 페이지 표시 예: `⚠️ 하자 신고 5건 · 하자율 6% (배송 4 · 변질 1)`. **랭킹에는 반영하지 않는다.**

---

## 4. DB 스키마 (Supabase)

### `kimchi` — 김치 상품 (관리자 등록)

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | uuid, PK | |
| name | text | 상품명 |
| brand | text | 브랜드/제조사 |
| image_url | text | |
| category | text | 배추김치, 깍두기, 열무김치 등 |
| competition_year | int | 품평회 연도 (예: 2025) |
| competition_award | text | 대상, 금상, 은상, 동상 등 |
| created_at | timestamptz | |

### `price_posts` — 딜/가격 게시물 (사용자·운영자 입력)

가격 데이터 + 딜(핫딜) 커뮤니티 레이어를 함께 담는다. 단순 최저가 제보 = 할인 강조 없는 딜. **최저가·가격 랭킹은 `status='active'`인 딜 중 최저 `price_per_100g`** 로 계산.

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | uuid, PK | |
| kimchi_id | uuid, FK → kimchi.id | **딜↔랭킹 연결 키** (딜에서 이 김치의 랭킹으로 이동) |
| user_id | uuid, FK → auth.users.id | 작성자 (운영자 큐레이션 포함) |
| title | text | 딜 제목 |
| body | text | 본문/설명 |
| discount_info | text | 원가·할인율·이벤트명 등 |
| price | int | 판매가 (원) |
| volume_g | int | 용량 (g) |
| price_per_100g | numeric | 자동 계산: price / volume_g × 100 |
| store_name | text | 판매처명 |
| purchase_url | text | 구매 링크 |
| purchase_method | text | 온라인 / 오프라인 / 앱 |
| ends_at | timestamptz NULL | 마감일 (시효) |
| status | text | 'active'(진행중) / 'ended'(종료) / 'soldout'(품절) |
| upvote_count | int | 추천 수 (캐시) |
| comment_count | int | 댓글 수 (캐시) |
| view_count | int | 조회 수 |
| hot_score | numeric | 인기글 점수 (계산·캐시, 10.3) |
| is_official | boolean | 운영자 큐레이션 여부 |
| created_at | timestamptz | |

> `kimchi_id`가 랭킹↔딜 양방향 연결의 핵심: 상세(랭킹) 화면은 이 값으로 관련 핫딜을 조회하고, 딜 화면은 이 값으로 해당 김치 랭킹 정보로 이동한다.

### `deal_votes` — 딜 추천 (upvote)

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | uuid, PK | |
| price_post_id | uuid, FK → price_posts.id | |
| user_id | uuid, FK → auth.users.id | 추천한 사람 |
| created_at | timestamptz | |

> UNIQUE(price_post_id, user_id) · self-vote(자기 딜 추천) 금지  
> 딜 댓글은 `deal_comments`(price_post_id, user_id, body, created_at) 별도 테이블 — 상세 설계 시 추가

### `reviews` — 사용자 리뷰 (1인 1리뷰, 수정 가능)

리뷰 하나가 맛평가·제품평가를 각각/함께 담는다. 맛 점수 4개는 **nullable**, 하자 필드도 nullable.
- **맛평가 했다** = `score_overall IS NOT NULL` (별점이 맛평가의 필수·기준값)
- **맛 프로필 3축**(spicy/sweet/fishiness)은 별점을 남겨도 비어 있을 수 있음 (선택 입력)
- **제품평가만 함** = 맛 점수 전부 null + `defect_types` 존재

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | uuid, PK | |
| kimchi_id | uuid, FK → kimchi.id | |
| user_id | uuid, FK → auth.users.id | |
| score_overall | numeric(2,1) NULL (1.0~5.0, 0.5 단위) | 맛평가: **종합 만족도 (품질 — 랭킹 기준)** |
| score_spicy | int NULL (1~10) | 맛 프로필: 매운맛 |
| score_sweet | int NULL (1~10) | 맛 프로필: 단맛 |
| score_fishiness | int NULL (1~10) | 맛 프로필: 젓갈맛 |
| comment | text NULL | 맛평가 코멘트 |
| defect_types | text[] NULL | 제품평가: 하자 유형 목록 (있으면 하자 신고 = 있음) |
| defect_note | text NULL | 제품평가: 하자 설명 |
| defect_image_url | text NULL | 제품평가: 하자 사진 (**향후**) |
| created_at | timestamptz | |
| updated_at | timestamptz | |

> UNIQUE(kimchi_id, user_id)  
> CHECK: `score_overall IS NOT NULL OR defect_types IS NOT NULL` — 맛평가·제품평가 중 최소 하나는 있어야 함  
> 집계 규칙: 만족도 랭킹은 `score_overall IS NOT NULL`인 행만, 하자율은 `defect_types IS NOT NULL`인 행을 분자로 사용

### `profiles` — 사용자 프로필 (Supabase Auth 확장)

| 컬럼 | 타입 | 설명 |
|------|------|------|
| user_id | uuid, PK, FK → auth.users.id | |
| nickname | text | |
| reputation_score | int | 누적 신뢰도 점수 |
| ranker_tier | text | 'general' \| 'trusted' \| 'ranker' |
| updated_at | timestamptz | |

### `review_likes` — 리뷰 공감

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | uuid, PK | |
| review_id | uuid, FK → reviews.id | |
| user_id | uuid, FK → auth.users.id | 공감을 누른 사람 |
| created_at | timestamptz | |

> UNIQUE(review_id, user_id)

---

## 5. 랭킹 계산 로직

### 시간 기반 가중치 — 동적 구간

**Step 1. 구간 결정**
- 최근 3개월 리뷰 수 >= 5 → 3개월 구간 사용
- 최근 6개월 리뷰 수 >= 5 → 6개월 구간 사용
- 전체 리뷰 수 < 5 → 전체 기간 단순 평균

**Step 2. 구간 내 가중치**
- 최근 3개월 × 3.0
- 3~6개월 × 2.0
- 6개월~1년 × 1.0

### 만족도 점수 계산 (시간·랭커 가중 + 리뷰 수 보정)

랭킹의 기준은 **종합 만족도(score_overall)** 다. 다만 리뷰가 적은 김치(별점 5.0짜리 리뷰 1개 등)가 극단값으로 상위를 차지하지 않도록, 시간·랭커 가중 평균에 **베이지안 평균(shrinkage) 보정**을 더한다.

**Step 1. 가중 합 구하기**
```
W = Σ(시간가중치 × 랭커가중치)                      ← 가중치 총합
S = Σ(score_overall × 시간가중치 × 랭커가중치)       ← 가중 점수 합
```

**Step 2. 베이지안 보정**
```
만족도 점수 = (C × m + S) / (C + W)
```
- **m** = 전체 김치의 전역 평균 만족도 (prior 기준값)
- **C** = prior 강도 = "가상 리뷰 가중치". 초깃값 예: **5**

리뷰 가중치가 거의 없으면(W ≪ C) 점수가 전역 평균 m 쪽으로 당겨지고, 리뷰가 충분히 쌓이면(W ≫ C) 자기 가중 평균에 수렴한다. → 소수 리뷰 제품이 극단 별점으로 1위를 먹는 것을 방지.

> **C는 실데이터 확인 후 조정** (미확정). C가 클수록 신생·소수리뷰 제품을 더 보수적으로 취급. m은 전체 리뷰 평균으로 주기적 재계산.

> 맛 프로필 3축(매운맛·단맛·젓갈맛)도 시간·랭커 가중 평균을 내지만, 이는 **레이더 차트 표시용**일 뿐 랭킹 계산에는 쓰지 않는다 (보정도 불필요).

### 정규화 (0~1 스케일 맞추기)

전체 랭킹은 성격이 다른 두 점수를 섞으므로 각각 0~1로 정규화한 뒤 가중합한다.

- **만족도 정규화**: `(만족도 점수 − 1) / (5 − 1)` → 별점 1.0=0, 5.0=1

- **가격 정규화 (쌀수록 높음) — 같은 카테고리 내에서**:
  같은 `category`(배추김치, 깍두기 등) 안에서만 최저/최고를 잡아 정규화한다. 가격 기준은 김치별 최저 `price_per_100g`.
  ```
  가격 점수 = (카테고리_최고가 − 해당가) / (카테고리_최고가 − 카테고리_최저가)
  ```
  → 카테고리 내 최저가=1, 최고가=0. **"그 김치 종류 안에서 얼마나 저렴한가"** 를 의미한다.

  **엣지 케이스 처리**
  - 카테고리에 가격 있는 제품이 **1개뿐**(최고가=최저가)이면 정규화 불가 → 가격 점수 = **0.5 (중립)**.
  - 가격 게시물이 **아예 없는** 김치 → 가격 점수 없음 → **전체·가격·가성비 랭킹에서 제외**, 맛(만족도) 랭킹에만 노출.

> 첫 화면 전체 랭킹은 카테고리를 섞어 한 리스트로 보여주므로, 가격 점수는 "카테고리 내 상대 저렴함"이라는 의미로 섞인다. 추후 카테고리 필터 뷰를 넣으면 그 뷰 안에서 자연스럽게 동일 카테고리 비교가 된다.

### 랭킹별 계산

| 랭킹 | 계산식 |
|------|--------|
| **전체 랭킹** (첫 화면 기본) | **만족도(정규화) × 0.7 + 가격(정규화) × 0.3** |
| 맛 랭킹 | 만족도 점수 높은 순 |
| 가격 랭킹 | price_per_100g 오름차순 (싼 순) |
| 가성비 랭킹 | 만족도 점수 / price_per_100g (0~1 정규화 후 비교) |
| 품평회 | competition_award 기준 (대상 > 금상 > 은상 > 동상) |

> **하자율은 어떤 랭킹에도 반영하지 않는다** (제조·배송·포장 사고가 제품 본연의 맛 평가를 왜곡하지 않도록). 상세·목록에 정보 배지로만 노출.

---

## 6. 랭커(Ranker) 시스템 — 신뢰도 기반 평가 가중치

### 6.1 개념

사용자의 기여도(리뷰, 가격 게시물)와 타인으로부터 받은 공감을 합산해 신뢰도 점수를 계산한다. 일정 점수 이상이면 "랭커" 등급을 부여하고, 랭커의 리뷰는 점수 계산 시 일반 사용자보다 더 큰 비중을 가진다.

별도의 추천 섹션이나 발굴자 표시는 두지 않는다 — 랭커가 후하게 평가한 신규 제품은 가중치 덕분에 기존 랭킹(전체/맛/가성비)에서 자연스럽게 상위로 노출된다.

### 6.2 신뢰도 점수 계산 (초안 — 가중치는 추후 조정)

| 행동 | 점수 |
|------|------|
| 리뷰 작성 | +2 |
| 가격 게시물 등록 | +1 |
| 내 리뷰가 받은 공감 1개당 | +1 |
| 내 가격 게시물이 받은 공감 1개당 | +1 |

```
reputation_score = 기여도 점수 합 + 공감 점수 합
```

### 6.3 등급(Tier)

| 점수 범위 | 등급 | 리뷰 가중치 |
|-----------|------|-------------|
| 0 ~ 99 | 일반 사용자 (general) | 1.0 |
| 100 ~ 299 | 신뢰 사용자 (trusted) | 2.0 |
| 300 이상 | 랭커 (ranker) | 4.0 |

> 랭커 등급은 프로필에 배지로 표시

### 6.4 미확정 사항 (구현 전 결정 필요)

- 점수 가중치(+2, 1.0/2.0/4.0 등)는 초안이며 실사용 데이터 확인 후 조정 필요
- 자기 콘텐츠에 자기가 공감 누르는 것을 점수에서 제외하는 로직(self-like 방지) 필요
- 랭커 등급 강등 조건 (활동 중단 시 등급 유지 여부) 미정

---

## 7. 참고 자료

- **aT 대한민국 김치품평회** (제14회, 2025)
  - 주최: 한국농수산식품유통공사 (aT)
  - 심사: 전문가 + 일반 소비자 관능평가 + 현장 위생 평가
  - 수상: 대상, 금상, 은상, 동상
  - 결과: 매년 PDF 공개 → 관리자 수동 입력 방식

- **세계김치연구소 관능검사 매뉴얼**: https://www.wikim.re.kr

- **김치 관능평가 학술 기준**: 맛(신맛, 단맛, 감칠맛, 매운맛) + 식감(아삭함, 씹힘성) + 외관(색)
  - 출처: Journal of Sensory Studies, ResearchGate 등

---

## 8. 기술 스택

| 역할 | 기술 |
|------|------|
| Frontend | Flutter (멀티플랫폼: 웹 / Android / iOS) |
| Backend | Supabase (PostgreSQL + Auth + Storage + Edge Functions) |
| 차트 | 레이더 차트 (삼각형), 시계열 가격 차트 |
| 인증 | Supabase Auth |
| 이미지 저장 | Supabase Storage (CDN 포함 — 별도 이미지 서버 불필요) |

### 8.1 이미지 처리 방침

**별도 이미지 서버를 두지 않는다.** Supabase Storage가 오브젝트 저장소 + CDN 역할을 겸한다 (AWS의 S3 + CloudFront에 해당).

**버킷 구성**

| 이미지 | 버킷 | 접근 제어 (Storage RLS) |
|--------|------|-------------------------|
| 김치 상품 이미지 (`kimchi.image_url`) | public | 조회 누구나 / **업로드는 관리자만** |
| 하자 사진 (`defect_image_url`, 향후) | public 또는 private | 업로드는 로그인 유저 / 신고성이라 **검수 고려** |
| 프로필 이미지 (선택) | public | 본인만 업로드 |

**규칙**

- **DB에는 전체 URL이 아니라 `경로(path)`만 저장.** URL은 조회 시 `getPublicUrl(path)`로 생성 → 도메인·버킷이 바뀌어도 데이터 무영향. (private은 `createSignedUrl()`로 만료형 URL 발급)
- **업로드 전 클라이언트에서 압축·리사이즈** (`image_picker` + `flutter_image_compress`) → 용량·전송량 절감
- **표시는 `cached_network_image`** 로 앱 내 캐싱
- 업로드 시 `cacheControl` 지정해 CDN 캐시 활용
- **Storage RLS 정책 필수** — 미설정 시 아무나 업로드 가능
- ⚠️ **쇼핑몰 상품 이미지 핫링크 금지** (저작권·안정성) → 직접 받아 우리 Storage에 업로드

**미확정 / 주의**

- 썸네일: Supabase **이미지 변환(transform) 기능은 유료(Pro) 플랜** → 요금 확인 필요. 무료로 갈 경우 업로드 시 클라이언트에서 여러 크기를 미리 생성해 올리는 방식 검토
- 무료 티어의 저장 용량·월 전송량(egress) 한도에 먼저 부딪힐 수 있음 (수치는 공식 요금 정책 확인)
- 트래픽 증가 시 앞단 Cloudflare 또는 전용 이미지 CDN(Cloudinary 등) 이전 검토 — **초기엔 불필요**

---

## 9. 초기 데이터 확보 전략 (Go-to-Market)

### 9.1 콜드 스타트 문제

평가 데이터가 없으면 랭킹이 비어 무의미하다. 게다가 5장 **베이지안 보정** 때문에 리뷰가 적은 초기엔 모든 점수가 전역 평균으로 당겨져 **랭킹이 평평**해진다. → 초기 전략의 핵심은 (1) 평가를 특정 제품에 **모으고**, (2) 평가가 쌓이기 전까지 **UX로 우회**하는 것.

데이터는 두 종류:

| 종류 | 내용 | 확보 방법 | 난이도 |
|------|------|-----------|--------|
| 상품 카탈로그 | 김치 제품 목록 | 관리자 수동 씨딩 | 쉬움 |
| 평가 데이터 | 리뷰(별점 등) | 커뮤니티 이벤트 | 어려움 (콜드 스타트) |

### 9.2 상품 카탈로그 씨딩

- 출처: aT 품평회 수상작 + 온라인몰(쿠팡·컬리·네이버) 베스트셀러 + 유명 브랜드(종가집·비비고 등)
- 초기 **50~100개**, 스프레드시트 → CSV → Supabase import
- ⚠️ 사실 정보(이름·브랜드·용량)만 입력. **외부 몰의 별점/리뷰를 긁어와 우리 리뷰인 척 넣지 않는다** (척도 상이·약관·신뢰성 문제)

### 9.3 콜드 스타트 UX 우회 — 품평회 랭킹 먼저

- 리뷰가 쌓이기 전, 첫 화면 기본 = **품평회 랭킹** (평가 없어도 볼거리 제공)
- 김치당 리뷰가 임계치(예: **5개**) 넘으면 그 제품부터 **종합 랭킹** 노출로 전환

### 9.4 타겟 채널

- 1순위: **김치 전문 네이버 카페** (일반 맘카페보다 타겟 적합성이 압도적)
- 후보: 「**김치마스터(김치연구소)**」 — 회원 1만+, 2015 개설, 김치 전용 게시판 다수
  - ✅ 타겟 완벽(진짜 애호가), 글 내용이 곧 **제품 니즈 검증 자료** (선택 장애·숙성 변질·수입 죄책감 등)
  - ⚠️ **조회수 한 자릿수 = 실활동 수십 명 규모.** 리뷰 수백 개 기대 금물
- → **1곳으론 규모 부족.** 유사 김치/요리 카페 2~3곳 병행을 전제로 한다

### 9.5 이벤트 설계

**골자:** 웹 접속 → 지정 김치 중 먹어본 5개 평가 → 소셜 로그인 회원 확인 → 성실 리뷰 보상

| 요소 | 결정 |
|------|------|
| 접속 | Flutter **웹 링크** (앱 설치 강요 X — 이탈 방지) |
| 로그인 | 카카오/네이버 **소셜 로그인** (이메일 가입 X — 다계정 방지) |
| 평가 대상 | **이벤트 지정 김치 10종** 중 먹어본 **5개** (분산 방지·집중) |
| 필수 입력 | 별점 + **코멘트 1~2줄**(진정성 게이트). 맛 프로필 3축은 선택 |
| 보상 | 성실 리뷰 전원 **소액**(예: 커피 3천원) + 우수 리뷰 **추첨** 상위 상품 |
| 예산 | 참여 수십 명 가정, **상한 선지정** (예: 3천 × 40 ≈ 12만원) |

**함정 방어**

- **진정성**: 코멘트 필수 / 초고속 연속 제출 차단 / 이벤트 리뷰 `is_event` 태그 → 편향 모니터링·필요시 가중치 조정
- **분산**: 지정 10종 풀로 리뷰 집중
- **어뷰징**: 소셜 로그인 + 카페 닉네임 대조(매니저 협조) + 1인 1회 + 지급 전 수동 검수

### 9.6 매니저 제휴

- "홍보"가 아니라 **"카페 공동 이벤트"** 로 프레이밍 → 승낙률↑
- 카페에 줄 이득: 회원 전용 기프티콘 이벤트(활성화) + **결과 랭킹을 카페에 공유**
- 무단 홍보글은 삭제·강퇴 리스크 → **반드시 사전 협의**

### 9.7 실행 순서

1. 카탈로그 50개 입력
2. 첫 화면 = 품평회 랭킹
3. 매니저 협의 → 카페 공동 이벤트 (지정 10종 · 웹 · 소셜 로그인 · 보상)
4. 리뷰 몰린 제품부터 종합 랭킹 전환
5. 유사 카페 2~3곳으로 병행 확장

### 9.8 미해결 과제

- **[신규] 카페 내 구체적 홍보 방법** — 게시글 형식/문구, 게시판 선택, 게시 시점·빈도, 고정 공지 여부, 참여 인증 방식 등. 별도 기획 필요.
- 임계치(리뷰 N개), 베이지안 C, 보상 단가/예산 상한은 실데이터 보고 조정
- 병행할 유사 카페 2~3곳 후보 리스트업

---

## 10. 핫딜 — 딜/할인 정보 기능

### 10.1 개념 — 리텐션 엔진

평가/랭킹은 **가끔** 하는 행동(데이터·신뢰 축적), 딜은 **자주** 확인하는 행동(트래픽·재방문). 이 둘이 서로를 돌리는 **두 바퀴** 구조다. 뽐뿌형 딜 게시판을 홈의 **별도 탭**으로 둔다: **[랭킹] 탭 / [핫딜] 탭**.

### 10.2 랭킹 ↔ 딜 양방향 연결

탭은 분리하되 **김치 단위로 연결**된다 (연결 키: `price_posts.kimchi_id`).

- **랭킹 → 딜**: 김치 상세(랭킹) 화면에서 그 김치의 **진행 중 핫딜 3~5개** 노출 → 탭하면 딜 상세/딜 탭으로 이동.
- **딜 → 랭킹**: 딜 상세에서 "이 김치 랭킹·평가 보기" → 해당 김치 상세(만족도·프로필·하자율)로 이동.

### 10.3 딜글 = `price_posts` 확장

기존 `price_posts`에 커뮤니티·시효 필드(제목·본문·할인정보·마감일·상태·추천·조회·hot_score·is_official)를 더해 딜 게시물로 쓴다 (4장 스키마 참고). 최저가·가격 랭킹은 **`status='active'`인 딜 중 최저 `price_per_100g`** 로 계산.

### 10.4 인기글(핫딜) 노출 — 시간 감쇠 랭킹

딜은 **시효성이 생명** → 평가 랭킹(베이지안)과 완전히 다른 로직. 오래된 딜은 추천이 많아도 밀려나야 한다.

```
hot_score = (upvote_count − 1) / (경과시간_h + 2)^gravity      (Hacker News 방식, gravity 초깃값 1.5)
```

- 정렬: `status='active'` 딜을 hot_score 내림차순
- **🔥핫딜 승격**: upvote 임계(예: 10) 넘으면 배지 + 상단 노출
- **어뷰징 방어**: upvote에도 6장 **랭커/신뢰도 가중치를 재활용**(셀러 자작 추천 방어), self-vote 금지

### 10.5 만료·상태 관리

- `ends_at`(마감일) 지나면 자동 `ended`
- 사용자 "종료/품절" 신고가 임계 넘으면 `ended`/`soldout`
- 종료된 딜은 인기글에서 빠지고 "지난 딜" 아카이브로

### 10.6 초기 딜 확보 — 운영자 큐레이션으로 시작 (딜의 콜드 스타트)

| 단계 | 방식 | 비고 |
|------|------|------|
| **초기** | **운영자 큐레이션** (`is_official=true`) | 쿠팡·컬리·네이버·브랜드몰 김치 할인을 직접 등록. 모든 딜사이트의 초기 방식 |
| 중기 | 공식 API 자동 수집 | 네이버쇼핑·쿠팡파트너스 API로 가격 추적 → 평소가 대비 하락 시 딜 후보. **스크래핑 금지, 공식 API만** |
| 성숙기 | 사용자 제보 | 커뮤니티 성숙 후 주력 |

### 10.7 주의 (리스크)

- **스크래핑 금지**: 쇼핑몰 무단 크롤링은 약관·법적 리스크. 반드시 공식 API.
- **어필리에이트 이해상충**: 쿠팡파트너스 등 제휴 링크로 수익화 시 "공정한 최저가"라는 신뢰와 충돌 가능 → **제휴 링크임을 투명하게 표시**.

### 10.8 미확정

- hot_score `gravity`, 🔥핫딜 upvote 임계, 종료 신고 임계 → 실데이터 조정
- `deal_comments` 테이블 상세 설계
- 딜 카테고리(할인/최저가/사은품 등) 세분화 여부
- 카탈로그에 없는 김치 딜 허용 여부(`kimchi_id` nullable화)
